# 📱 側邊菜單功能文檔

## 🎯 功能概述

舒苑飲食文化網站現已實現**全頁面側邊菜單**（Sidebar Navigation），所有 166 個頁面都包含從左邊滑出的響應式導航菜單。

## ✨ 核心功能

### 1. 漢堡菜單按鈕
- **位置**：導航欄右上角
- **外觀**：☰ 三條橫線按鈕
- **尺寸**：46 × 46 像素
- **動畫**：點擊時按鈕變成 ✕ 關閉圖標

### 2. 側邊菜單
- **位置**：屏幕左側，固定定位
- **寬度**：280 像素
- **菜單項目**：
  - 首頁
  - 到府私廚
  - 服務項目
  - 主廚介紹
  - 服務實例
  - 菜單方案
  - 嚴選食材
  - 服務地區
  - Q&A
  - 預約諮詢（分離式設計）

### 3. 動畫效果
- **打開動畫**：菜單從 `translateX(-100%)` 滑入到 `translateX(0)`
- **關閉動畫**：菜單滑出屏幕
- **轉換時間**：300 毫秒
- **緩動函數**：`var(--ease)` (cubic-bezier)

### 4. 蒙層效果
- **背景**：半透明黑色 `rgba(0, 0, 0, 0.5)`
- **位置**：整個屏幕
- **點擊行為**：點擊蒙層時自動關閉菜單
- **層級**：z-index 999（菜單在 1000）

## 🎨 設計細節

### 顏色
- **菜單背景**：`var(--bg)` (深灰色)
- **文字顏色**：`var(--muted)` (中等灰色)
- **懸停高亮**：`var(--gold)` (金色)
- **活躍邊框**：金色左邊框

### 字體與排版
- **字體**：Noto Serif TC
- **大小**：0.95 rem (約 15px)
- **字母間距**：0.08em
- **行高**：正常

### 間距
- **菜單項目內邊距**：1rem 1.5rem
- **項目間距**：自動
- **懸停時左邊距增加**：額外 0.5rem
- **頂部預留空間**：100px (讓出導航欄)

## 🔧 技術實現

### CSS 類名
```css
.sidebar-nav          /* 側邊菜單容器 */
.sidebar-nav.active   /* 菜單打開狀態 */
.sidebar-overlay      /* 蒙層背景 */
.sidebar-overlay.active /* 蒙層可見狀態 */
.nav-toggle          /* 漢堡菜單按鈕 */
.nav-toggle.active   /* 按鈕打開狀態 */
```

### JavaScript 事件
```javascript
// 漢堡菜單點擊
navToggle.addEventListener('click', () => { ... })

// 蒙層點擊關閉
sidebarOverlay.addEventListener('click', () => { ... })

// 菜單項目點擊後關閉
sidebarNav.querySelectorAll('a').forEach(link => {
    link.addEventListener('click', () => { ... })
})
```

### ARIA 無障礙標籤
- `aria-expanded="true/false"` - 指示菜單狀態
- `aria-label="開啟導覽"` - 屏幕閱讀器提示

## 📱 響應式行為

### 桌面設備 (≥ 769px)
- 漢堡菜單按鈕：**隱藏** (`display: none`)
- 側邊菜單：**隱藏** (視覺上不可見 `translateX(-100%)`)
- 導航菜單：頂部導航條顯示（原有設計）

### 移動設備 (≤ 768px)
- 漢堡菜單按鈕：**顯示** (`display: inline-flex`)
- 側邊菜單：**可交互**（可點擊打開）
- 導航菜單：頂部導航條隱藏

## 🎯 用戶交互流程

```
移動設備用戶體驗：
┌─────────────────────────────────────┐
│ 頁面加載                             │
│ (漢堡菜單按鈕可見)                  │
└────────┬────────────────────────────┘
         │ 點擊漢堡菜單
         ▼
┌─────────────────────────────────────┐
│ 菜單從左邊滑出 + 蒙層出現           │
│ 按鈕變成關閉圖標 (✕)               │
└────────┬────────────────────────────┘
         │ 選擇：
         ├─ 點擊菜單項目 → 導航到頁面 + 菜單自動隱藏
         ├─ 點擊蒙層 → 菜單隱藏
         └─ 點擊按鈕 → 菜單隱藏
         │
         ▼
┌─────────────────────────────────────┐
│ 菜單滑出，蒙層消失                  │
│ 按鈕恢復為漢堡圖標 (☰)             │
└─────────────────────────────────────┘
```

## 📊 頁面覆蓋

| 分類 | 頁面數 | 狀態 |
|------|--------|------|
| 主目錄 HTML 頁面 | 159 | ✅ 已實現 |
| Blog 目錄頁面 | 7 | ✅ 已實現 |
| **總計** | **166** | **✅ 100%** |

## 🧪 測試清單

- [x] CSS 側邊菜單樣式加載正確
- [x] 漢堡菜單按鈕在移動設備顯示
- [x] 漢堡菜單按鈕在桌面設備隱藏
- [x] 點擊漢堡菜單打開側邊菜單
- [x] 菜單從左邊滑出
- [x] 蒙層背景出現
- [x] 按鈕變成關閉圖標
- [x] 菜單項目可點擊
- [x] 點擊菜單項後自動隱藏菜單
- [x] 點擊蒙層隱藏菜單
- [x] 點擊按鈕關閉菜單
- [x] 所有 166 個頁面都有菜單
- [x] ARIA 無障礙標籤正確

## 🐛 已知問題與解決方案

### 問題 1: 菜單在某些設備上不顯示
**解決方案**：確保瀏覽器支援 CSS `transform` 和 `transition` 屬性（所有現代瀏覽器都支援）

### 問題 2: 動畫不流暢
**解決方案**：檢查瀏覽器性能，關閉其他標籤頁。側邊菜單使用 GPU 加速的 `transform` 屬性，性能應該很好。

### 問題 3: 蒙層點擊無效
**解決方案**：檢查 JavaScript console 是否有錯誤。確保 `sidebar-overlay` 元素存在於 DOM 中。

## 📝 維護提示

### 添加新菜單項目
在所有 HTML 文件中找到 `.sidebar-nav` 容器，並在其中添加新的 `<a>` 標籤：

```html
<div class="sidebar-nav">
    <a href="index.html">首頁</a>
    <!-- ... 現有項目 ... -->
    <a href="new-page.html">新頁面</a>  <!-- 新增 -->
</div>
```

### 修改菜單寬度
在 `css/style.css` 中修改 `.sidebar-nav` 的 `width` 屬性：

```css
.sidebar-nav {
    width: 320px;  /* 改為 320px */
}
```

### 更改菜單背景顏色
```css
.sidebar-nav {
    background: #1a1a1a;  /* 改為自訂顏色 */
}
```

### 調整動畫速度
```css
.sidebar-nav,
.sidebar-overlay {
    transition: ...0.5s... /* 改為 0.5s */
}
```

## 🔗 相關文件

- **CSS**：`css/style.css` (246-290 行)
- **JavaScript**：`js/main.js` (213-247 行)
- **HTML 模板**：`HTML_TEMPLATE_STANDARD.html`
- **驗證腳本**：`validate_sidebar.py`
- **實現腳本**：`add_sidebar_menu.py`

## 📞 支援

如有任何問題或需要進一步的功能增強，請參考以上文檔或檢查相關源文件。

---

**最後更新**：2024
**狀態**：✅ 全面實現，100% 測試通過
